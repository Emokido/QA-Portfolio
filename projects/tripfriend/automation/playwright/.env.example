# Copy these values into the current PowerShell session. Do not commit real credentials.
TF_E2E_WEB_URL=http://localhost:3000
TF_E2E_API_URL=http://localhost:8080
TF_E2E_USERNAME=
TF_E2E_PASSWORD=
# Existing local place ID used only for API fixture creation.
TF_E2E_PLACE_ID=1
# Exact accessible option name shown by the UI. Stage 7 seed example for ID 1:
TF_E2E_PLACE_NAME=경복궁 (서울)
